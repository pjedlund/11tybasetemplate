document.addEventListener('DOMContentLoaded', (event) => {
	//line + comment to be removed
	//line2 + comment2 to be removed
})

//line2 + comment2 to be removed
const unused = 1
const h1 = document.querySelector('h1')
h1.innerText += ' | Surprise!'
